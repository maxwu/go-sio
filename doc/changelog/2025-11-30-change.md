# Benchmark plan

- [x] Review existing reader implementations and tests to understand usage and edge cases to benchmark.
- [x] Design benchmark scenarios (StreamReader with/without filters, JSON filter, tee) and choose representative input sizes.
- [x] Implement benchmarks in `_test.go` and run `go test -bench` to validate and capture baselines.

## Notes

- Added table-driven StreamReader benchmarks for no-op filter, drop-half filter, and uppercase transform.
- Benchmarks remain for JSON filtering and teeing into a side buffer.
- Baseline (`go test -bench . -run '^$' ./...` on Apple M3 Pro):  
  - StreamReader/NoFilter: ~27.6µs/op  
  - StreamReader/FilterDropHalf: ~20.4µs/op  
  - StreamReader/FilterTransform: ~75.6µs/op  
  - JSONFilter: ~109µs/op  
  - TeeReader: ~7.5µs/op
